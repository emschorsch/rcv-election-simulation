# Python
__pycache__/
.pytest_cache/

# LLM extraction cache (Claude PDF→tidy results, hashed by source URL).
# Safe to delete; will be re-fetched (and re-billed) on next run.
.cache/

# Note: the generated election workbooks (Top_RCV_Races.xlsx and the five
# per-source files) used to be ignored, but are now tracked so anyone
# cloning the repo can read the curated findings without re-running the
# whole pipeline. Regeneration overwrites them in place.

# Editor / IDE
.idea/

# Claude Code session state
.claude/
